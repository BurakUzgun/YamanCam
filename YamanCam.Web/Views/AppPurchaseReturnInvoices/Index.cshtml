@model YamanCam.Web.Models.AppPurchaseInvoiceListViewModel
@using YamanCam.Web.Services
@{
    ViewData["Title"] = "Alış İade Faturaları";
    var kusurat = ViewData["Kusurat"] as IReadOnlyDictionary<string, int>;
    int Dec(string key) => NumberFormatHelper.Get(kusurat, key);
}

<div class="card border-0 shadow-sm">
    <div class="card-body">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <div>
                <h1 class="h4 mb-1">Alış İade Faturaları</h1>
                <p class="text-muted mb-0">Alış iade faturası kayıtlarını bu ekrandan yönetebilirsiniz.</p>
            </div>
            <a asp-action="Create" class="btn btn-primary btn-sm">Yeni Alış İade Faturası</a>
        </div>

        @if (TempData["SuccessMessage"] is string successMessage)
        {
            <div class="alert alert-success">@successMessage</div>
        }
        @if (TempData["ErrorMessage"] is string errorMessage)
        {
            <div class="alert alert-danger">@errorMessage</div>
        }

        <form method="get" class="mb-2">
            <div class="form-check">
                <input type="checkbox"
                       class="form-check-input"
                       id="showDeleted"
                       name="showDeleted"
                       value="true"
                       @(Model.ShowDeleted ? "checked" : "")
                       onchange="this.form.submit()" />
                <label class="form-check-label" for="showDeleted">Silinenler</label>
            </div>
        </form>

        <div class="border rounded overflow-hidden">
            <div class="d-flex justify-content-end p-2 border-bottom bg-light">
                <button type="button" class="btn btn-sm btn-outline-secondary" id="purchaseReturnInvoiceClearFilters">Filtreleri Temizle</button>
            </div>

            <div class="table-responsive">
                <table class="table table-sm table-hover align-middle mb-0" id="purchaseReturnInvoiceGrid">
                    <thead>
                        <tr>
                            <th class="pi-col-no">Fatura No</th>
                            <th class="pi-col-date">Fatura Tarihi</th>
                            <th class="pi-col-date">İşlem Tarihi</th>
                            <th>Şirket</th>
                            <th>Şube</th>
                            <th>Fatura Carisi</th>
                            <th>Döviz</th>
                            <th class="text-end">Kur</th>
                            <th class="text-end">Toplam (Döviz)</th>
                            <th class="text-end">Toplam (TL)</th>
                            <th class="pi-col-desc">Açıklama</th>
                            <th></th>
                        </tr>
                        <tr class="table-light">
                            <th><input type="text" class="form-control form-control-sm column-filter" data-col="0" placeholder="Ara..." /></th>
                            <th><input type="text" class="form-control form-control-sm column-filter" data-col="1" placeholder="Ara..." /></th>
                            <th><input type="text" class="form-control form-control-sm column-filter" data-col="2" placeholder="Ara..." /></th>
                            <th><input type="text" class="form-control form-control-sm column-filter" data-col="3" placeholder="Ara..." /></th>
                            <th><input type="text" class="form-control form-control-sm column-filter" data-col="4" placeholder="Ara..." /></th>
                            <th><input type="text" class="form-control form-control-sm column-filter" data-col="5" placeholder="Ara..." /></th>
                            <th><input type="text" class="form-control form-control-sm column-filter" data-col="6" placeholder="Ara..." /></th>
                            <th></th>
                            <th></th>
                            <th></th>
                            <th><input type="text" class="form-control form-control-sm column-filter" data-col="10" placeholder="Ara..." /></th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.Items.Count == 0)
                        {
                            <tr class="empty-data-row">
                                <td colspan="12" class="text-center text-muted">Kayıt bulunamadı.</td>
                            </tr>
                        }
                        else
                        {
                            foreach (var item in Model.Items)
                            {
                                <tr class="data-row">
                                    <td class="pi-col-no">@item.InvoiceNo</td>
                                    <td class="pi-col-date">@item.InvoiceDate.ToString("dd.MM.yyyy")</td>
                                    <td class="pi-col-date">@item.TransactionDate.ToString("dd.MM.yyyy")</td>
                                    <td>@(item.CompanyName ?? "-")</td>
                                    <td>@(item.WorkPlaceName ?? "-")</td>
                                    <td>@(item.AccountName ?? "-")</td>
                                    <td>@item.CurrencyCode</td>
                                    <td class="text-end">@item.ExchangeRate.ToString(NumberFormatHelper.Format(Dec("Alış Faturaları Kur")))</td>
                                    <td class="text-end">@item.TotalAmount.ToString(NumberFormatHelper.Format(Dec("Alış Faturaları Toplam (Döviz)")))</td>
                                    <td class="text-end">@item.TotalAmountTRY.ToString(NumberFormatHelper.Format(Dec("Alış Faturaları Toplam (TL)")))</td>
                                    <td class="pi-col-desc">@(item.Description ?? "-")</td>
                                    <td class="text-end text-nowrap">
                                        @if (Model.ShowDeleted)
                                        {
                                            <form asp-action="Restore" asp-route-id="@item.RecId" method="post" class="d-inline">
                                                @Html.AntiForgeryToken()
                                                <button type="submit"
                                                        class="btn btn-sm btn-outline-success"
                                                        onclick="return confirm('Bu kaydı geri yüklemek istediğinize emin misiniz?');">
                                                    Geri Yükle
                                                </button>
                                            </form>
                                        }
                                        else
                                        {
                                            <a asp-action="Edit" asp-route-id="@item.RecId" class="btn btn-sm btn-outline-primary">Düzenle</a>
                                            <form asp-action="Delete" asp-route-id="@item.RecId" method="post" class="d-inline">
                                                @Html.AntiForgeryToken()
                                                <button type="submit"
                                                        class="btn btn-sm btn-outline-danger"
                                                        onclick="return confirm('Bu kaydı silmek istediğinize emin misiniz?');">
                                                    Sil
                                                </button>
                                            </form>
                                        }
                                    </td>
                                </tr>
                            }
                            <tr id="purchaseReturnInvoiceNoFilterResults" class="d-none">
                                <td colspan="12" class="text-center text-muted">Filtreye uygun kayıt bulunamadı.</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<style>
    #purchaseReturnInvoiceGrid .pi-col-no {
        min-width: 120px;
        width: 12%;
    }

    #purchaseReturnInvoiceGrid .pi-col-date {
        min-width: 110px;
        width: 10%;
    }

    #purchaseReturnInvoiceGrid .pi-col-desc {
        min-width: 200px;
        width: 18%;
    }
</style>

@section Scripts {
    <script>
        (function () {
            var grid = document.getElementById('purchaseReturnInvoiceGrid');
            if (!grid) return;

            var filters = grid.querySelectorAll('.column-filter');
            var dataRows = grid.querySelectorAll('tbody tr.data-row');
            var noResultsRow = document.getElementById('purchaseReturnInvoiceNoFilterResults');
            var clearButton = document.getElementById('purchaseReturnInvoiceClearFilters');

            function applyFilters() {
                var visibleCount = 0;

                dataRows.forEach(function (row) {
                    var visible = true;

                    filters.forEach(function (input) {
                        var query = (input.value || '').toLowerCase().trim();
                        if (!query) return;

                        var colIndex = parseInt(input.getAttribute('data-col'), 10);
                        var cell = row.cells[colIndex];
                        var text = (cell ? cell.textContent : '').toLowerCase();
                        if (text.indexOf(query) === -1) {
                            visible = false;
                        }
                    });

                    row.classList.toggle('d-none', !visible);
                    if (visible) visibleCount++;
                });

                if (noResultsRow) {
                    noResultsRow.classList.toggle('d-none', visibleCount > 0);
                }
            }

            filters.forEach(function (input) {
                input.addEventListener('input', applyFilters);
            });

            if (clearButton) {
                clearButton.addEventListener('click', function () {
                    filters.forEach(function (input) {
                        input.value = '';
                    });
                    applyFilters();
                });
            }
        })();
    </script>
}
